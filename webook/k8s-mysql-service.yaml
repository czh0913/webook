apiVersion: v1
kind: Service
metadata:
  name: webook-mysql
spec:
  selector:
    app: webook-mysql
  ports:
    - protocol: TCP
      # 访问的端口
      port: 3306
      # pods 端口
      targetPort: 3306
      nodePort: 30001
  type: NodePort
  